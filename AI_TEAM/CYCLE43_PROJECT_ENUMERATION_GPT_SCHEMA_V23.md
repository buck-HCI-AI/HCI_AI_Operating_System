# CYCLE 43 — Project Enumeration Endpoint + GPT Schema v2.3
**GBT Cycle:** 43
**Sprint:** 8
**Priority:** HIGH — Unblocks GPT Project Lookup
**Status:** SPEC — Quick Build
**Date:** 2026-07-02
**Author:** Browser Claude (BC)

---

## Problem Statement

Field GPT returned Not Found on 1355 Riverside during Buck RFI review. Root cause: GPTs guess project codes with no way to verify. No endpoint exists to list available projects.

---

## Endpoint Required

### GET /gateway/projects
List all active project codes. No auth required.

Response:
- code, name, status, project_id for each active project
- Exclude project_id=28 (QATEST) per ADR-S8-002
- Redis cache 60s TTL

Expected result: 101F, 1355R, 64EW, 246GW

---

## GPT Schema v2.3

After /gateway/projects is live, BC updates schema adding 8 new tools:
1. getProjects -> GET /gateway/projects
2. getPortfolioSummary -> GET /gateway/portfolio/summary
3. getPortfolioRisks -> GET /gateway/portfolio/risks
4. getPortfolioDecisions -> GET /gateway/portfolio/decisions
5. getPortfolioProcurement -> GET /gateway/portfolio/procurement
6. getProjectRFIs -> GET /gateway/project/{code}/rfis
7. getProjectDrawings -> GET /gateway/project/{code}/drawings
8. getMorningBrief -> GET /gateway/project/{code}/morning-brief

Total after v2.3: 30 tools (22 existing + 8 new)

---

## System Prompt Updates

HCI Field GPT: Add rule - call getProjects first if project code is uncertain. Never guess.

HCI Chief Architect: Add rule - call getWarmStart + getBuckTelegramMessages at every session start before responding.

---

## Implementation Steps

1. Code: Add GET /gateway/projects to projects router
2. SQL: SELECT code, name, status, id FROM projects WHERE status=active AND id != 28
3. Redis cache 60s
4. Tests: 109/109 still pass
5. Code posts ai_message to BC when live
6. BC: Update HCI_AI_CustomGPT_Schema.json to v2.3
7. BC: Reload schema into all 5 GPTs via Import from URL
8. BC: Test Field GPT project enumeration

---

## Success Criteria

- GET /gateway/projects returns 4 active projects
- QATEST excluded
- Field GPT no longer guesses project codes
- Schema v2.3 in all 5 GPTs
- No more Not Found errors from wrong codes
- 109/109 tests pass

---

*Generated by Browser Claude — 2026-07-02 — Per never-stop directive*
