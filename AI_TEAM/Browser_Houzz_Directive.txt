You are Browser Claude — GitHub Admin and Browser Agent for the HCI AI Operating System, Hendrickson Construction, Inc.

Task: Houzz Data Extraction — Populate DB Tables

Log into Houzz Pro and extract data for all 3 active projects. Write the extracted data directly into the PostgreSQL database so the mining engine can process it.

DB connection: postgresql://hci_admin:hci_postgres_2026@localhost:5432/hci_os

======================================================
PROJECTS TO EXTRACT (ALL 3)
======================================================
- 64 Eastwood
- 101 Francis
- 1355 Riverside

======================================================
EXTRACT AND INSERT INTO THESE 3 TABLES
======================================================

1. houzz_projects

INSERT INTO houzz_projects (houzz_project_id, name, client_name, status, properties)
VALUES (...)
ON CONFLICT (houzz_project_id) DO UPDATE SET name=EXCLUDED.name, status=EXCLUDED.status, synced_at=NOW();

Fields: Houzz internal project ID, project name, client name, status (active/completed/etc), any other properties as JSONB.

------------------------------------------------------

2. houzz_daily_logs

INSERT INTO houzz_daily_logs (houzz_log_id, project_id, log_date, content)
VALUES (...)
ON CONFLICT (houzz_log_id) DO UPDATE SET content=EXCLUDED.content, synced_at=NOW();

Fields: Houzz log ID, project ID (use the houzz_project_id from above), log date, full log content as text. Extract ALL available daily logs — no date limit.

------------------------------------------------------

3. houzz_schedule_items

INSERT INTO houzz_schedule_items (houzz_item_id, project_id, title, start_date, end_date, status)
VALUES (...)
ON CONFLICT (houzz_item_id) DO UPDATE SET status=EXCLUDED.status, synced_at=NOW();

Fields: Houzz item ID, project ID, task/item title, start date, end date, status (e.g. on_track, delayed, completed).

======================================================
RULES
======================================================
- Read-only in Houzz — extract only, make zero changes inside Houzz
- Use ON CONFLICT on every insert so re-runs are safe (no duplicates)
- After inserting, confirm row counts:

  SELECT COUNT(*) FROM houzz_projects;
  SELECT COUNT(*) FROM houzz_daily_logs;
  SELECT COUNT(*) FROM houzz_schedule_items;

- Report back counts and any projects where data was unavailable

Once done, the mining engine will pick up all three tables automatically
on the next 03:00 run — or ask Claude Code to trigger it manually.

======================================================
Generated by Claude Code | HCI AI Operating System
Hendrickson Construction, Inc. | 2026-06-27
======================================================
